# arch-installer

Instructions

Get the latest Archlinux iso

Boot into the Archlinux iso

Once booted execute the following command
`timedatectl set-ntp true`

Find the name of the device to install Archlinux on
`lsblk`
